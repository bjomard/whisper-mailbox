/target
*.db
*.db-wal
*.db-shm
.env
.DS_Store
/ens/.env

# env / secrets
.env
.env.*
!.env.example
**/.env
**/*.pem
**/*.key
**/*secret*
**/*private*
**/*mnemonic*
**/*wallet*

# logs / db
*.log
*.db
*.db-wal
*.db-shm

# node
node_modules/

# build
dist/
target/


# Secrets / env (NEVER COMMIT)
.env
.env.*
*.bak
*secret*
*private*key*
*mnemonic*
*seed*
*wallet*

# Node deps
**/node_modules/

# Local DB / runtime artifacts
mailbox.db*

# local: contactcards is its own repo
/contactcards/

# build artifacts
/ens/dist/
/ens/dist-old/

# contactcards is a separate repo (GitHub Pages)
/contactcards/

# local backups / patches
ens/profiles_legacy/
ens/*.patch
ens_patch.diff
ens/scripts/*.bak*
ens/scripts/delegation_body.json
ens/scripts/sign_delegation.js
ens/scripts/hash_delegation.js

# contactcards is a separate repo (GitHub Pages)
/contactcards/

# contactcards is a separate repo (GitHub Pages)
/contactcards/

# contactcards is a separate repo (GitHub Pages)
/contactcards/
